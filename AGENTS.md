# 项目协作规范

## 基本规则

- 本仓库分析 MATPOWER 的 `case33bw` 33 节点配电网算例。
- 不修改、不复制 MATPOWER 源文件到本仓库。
- 所有结果必须由 MATLAB 脚本重新生成，禁止手动编造或修改结果数据。
- 假设、单位、数据来源以及 MATLAB/MATPOWER 版本必须记录清楚。
- 潮流失败或不收敛的场景必须保留并标记，不能删除。
- 不能为了得到可行方案而临时放宽电压约束。
- 本项目必须明确表述为个人仿真分析，不得包装成真实工程项目。

## 中文表达规范

- README、阶段报告、状态文件、数据来源说明全部使用中文。
- 图表标题、坐标轴、图例、注释和示例必须使用中文，并设置可用的中文字体。
- 文件名和 MATLAB 函数名使用逻辑清晰的英文，名称应能说明用途，例如 `run_baseline.m` 表示“运行基准潮流”。
- CSV 保留便于脚本处理的英文变量名，同时在报告中提供中文字段解释。
- 每个阶段完成后，必须生成一份中文阶段分析，包含实际结果、工程解释、面试讲法和 GitHub 上传清单。

## 项目范围

当前版本包括稳态交流潮流、等效有功光伏注入、节点/容量扫描、三种负荷水平、电压约束筛选、图表、表格和面试材料。

不包括储能、光伏时序出力、无功电压控制、保护、短路计算、OPF、优化算法、动态 Simulink 模型和实际电网数据。

## MATLAB 工作流

1. 在 `config/project_config.m` 中配置 MATPOWER 路径，或使用已保存的 MATLAB 路径。
2. 运行 `scripts/startup_project.m`。
3. 按顺序运行：基准潮流、单点验证、批量扫描、方案排序、图表生成、结果导出。
4. 每个验收通过的里程碑都要更新 `PROJECT_STATUS.md` 和对应中文报告。
